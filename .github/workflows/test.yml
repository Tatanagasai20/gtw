name: hello world
on: workflow_dispatch
jobs:
  task1:
    runs-on: ubuntu-latest
    steps:
      - name: hello step
        run: |
          echo "this is line one"
          echo "this is line two"
