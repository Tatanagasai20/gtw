name: hello world
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build step
        run: echo "build step"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test step
        run: echo "test phase"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: deploy stage
        run: echo "deploying the app"
