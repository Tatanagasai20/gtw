name: sample
on:
  push:
    branches:
      - main
  pull_request:
    types: opened
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: building stage
        run: echo "Hello World"
